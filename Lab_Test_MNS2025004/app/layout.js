export const metadata = {
  title: 'Diffie-Hellman Key Exchange',
  description: 'Establish shared secret key using Diffie-Hellman protocol',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}